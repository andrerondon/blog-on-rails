<h1 class='shadow-sm p-3 mb-5 bg-white rounded overflow-wrap'>INDIVIDUAL POST</h1>

<div class='shadow-sm p-3 mb-5 bg-white rounded overflow-wrap'>
  <h1>
    <span class='badge badge-pill badge-info'>
      <%= @post.id %>
    </span>
    <%= @post.title %>
  </h1>
  <p class='shadow-none p-3 mb-5 bg-light rounded'><%= @post.body %></p>

  <%# link_to arguments: name, path, options_object %>
  <% if @post %>
  <div>
    <%= link_to(
      "Edit",
      edit_post_path(@post),
      class: 'btn btn-info'
    )
    %>
    <%= link_to(
      "Delete",
      post_path(@post),
      method: :delete,
      class: 'btn btn-danger',
      data: { confirm: 'are you sure?' }
    )
    %>
  </div>
  <% end %>
</div>




